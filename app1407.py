#use of rarge()
def testRange():

    values = range(1,12+1)
    # values = range(4)
    #iterate from i = 0 to i =3
    print('สูตรคูณเเม่ ' + str(n))
    # values = range(1, 12+1)
    for i in values:
        print("%d * %d = %d" %(n,i,n*i))